{
  "pluginAlias": "Sonos",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Sonos",
        "placeholder": "e.g. Sonos",
        "description": "Name for this Sonos connection."
      },
      "hostname": {
        "type": "string",
        "format": "hostname",
        "title": "Speaker Hostname",
        "required": false,
        "default": "",
        "placeholder": "e.g. 192.168.96.56",
        "description": "In some network situations (or Docker usage) SSDP won't work, but you can also initialize the platform if you know one (static) IP of a single speaker."
      }
    }
  }
}
